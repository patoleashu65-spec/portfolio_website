# Portfolio — patoleashu65-spec

A clean, dark-themed React portfolio website.

## 🚀 Deploy to Vercel

### Option 1 — Vercel CLI
```bash
npm install
npm run build
npx vercel --prod
```

### Option 2 — GitHub Import (recommended)
1. Push this folder to a GitHub repo
2. Go to [vercel.com](https://vercel.com) → **New Project**
3. Import your repo
4. Leave all settings as default (Vercel auto-detects Create React App)
5. Click **Deploy** ✅

## 🛠 Development
```bash
npm install
npm start       # runs on http://localhost:3000
```

## ✏️ Customizing

All editable content is at the top of `src/App.js`:

- **SKILLS** — update the skill names and percentages
- **PROJECTS** — replace the placeholder projects with your real repos
- **GITHUB_USERNAME** — already set to `patoleashu65-spec`

To add your profile photo, GitHub will auto-load it from:
`https://github.com/patoleashu65-spec.png`
